tests:
  - description: "A greeting should get printed"
    configuration:
      tasks: tasks
    expectation:
      output_contains:
        - npmPublish
        - npmLogin
        - npmLoginSetRegistry
        - npmLoginSetToken
        - liftPackageJson
      success: tasks
